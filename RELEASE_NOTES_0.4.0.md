# Release Notes - v0.4.0

## 🚀 New Features

### Agents API
Added support for the Corti Agentic Framework with full CRUD operations:
- `agents.list()`, `agents.create()`, `agents.get()`, `agents.update()`, `agents.delete()`
- `agents.messageSend()`, `agents.getTask()`, `agents.getContext()`, `agents.getRegistryExperts()`

Learn more: [Corti Agentic Framework Documentation](https://docs.corti.ai/agentic/welcome)

### Document Generation
Added `disableGuardrails` parameter to document creation endpoints for more flexible content generation.

---

**Breaking Changes:** None
