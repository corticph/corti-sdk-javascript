// This file was auto-generated by Fern from our API Definition.

import * as Corti from "../../../src/api/index";
import { CortiClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";
import { mockOAuth } from "../mockAuth";

describe("RegistryClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = {
            connectors: [
                {
                    id: "@dedalus/coding-expert",
                    type: "registry",
                    name: "coding-expert",
                    title: "ICD-10 Coding Expert",
                    description: "description",
                    version: "1.4.2",
                    icons: [{ src: "src", mimeType: "image/svg+xml", sizes: ["48x48"] }],
                    provider: "Dedalus",
                    websiteUrl: "websiteUrl",
                    documentationUrl: "documentationUrl",
                    tags: ["tags"],
                    configSchema: { key: "value" },
                },
            ],
            nextPageToken: "nextPageToken",
            totalSize: 42,
        };

        server
            .mockEndpoint({ once: false })
            .get("/v2/agentic/registry/connectors")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            connectors: [
                {
                    id: "@dedalus/coding-expert",
                    type: "registry",
                    name: "coding-expert",
                    title: "ICD-10 Coding Expert",
                    description: "description",
                    version: "1.4.2",
                    icons: [
                        {
                            src: "src",
                            mimeType: "image/svg+xml",
                            sizes: ["48x48"],
                        },
                    ],
                    provider: "Dedalus",
                    websiteUrl: "websiteUrl",
                    documentationUrl: "documentationUrl",
                    tags: ["tags"],
                    configSchema: {
                        key: "value",
                    },
                },
            ],
            nextPageToken: "nextPageToken",
            totalSize: 42,
        };
        const page = await client.agentic.registry.list();

        expect(expected.connectors).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.connectors).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/agentic/registry/connectors")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.registry.list();
        }).rejects.toThrow(Corti.UnauthorizedError);
    });

    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = {
            id: "@dedalus/coding-expert",
            type: "registry",
            name: "coding-expert",
            title: "ICD-10 Coding Expert",
            description: "Returns ICD-10 codes for a clinical encounter.",
            version: "1.4.2",
            icons: [
                {
                    src: "https://cdn.corti.ai/registry/dedalus/coding-expert.svg",
                    mimeType: "image/svg+xml",
                    sizes: ["any"],
                },
            ],
            provider: "Dedalus",
            websiteUrl: "https://dedalus.example.com/coding-expert",
            documentationUrl: "https://docs.dedalus.example.com/coding-expert",
            capabilities: {
                streaming: true,
                inputModes: ["text/plain"],
                outputModes: ["text/plain", "application/json"],
                tools: ["lookup_icd10", "validate_code"],
            },
            tags: ["icd10", "billing", "expert"],
            configSchema: { key: "value" },
        };

        server
            .mockEndpoint()
            .get("/v2/agentic/registry/connectors/connectorId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agentic.registry.get("connectorId");
        expect(response).toEqual({
            id: "@dedalus/coding-expert",
            type: "registry",
            name: "coding-expert",
            title: "ICD-10 Coding Expert",
            description: "Returns ICD-10 codes for a clinical encounter.",
            version: "1.4.2",
            icons: [
                {
                    src: "https://cdn.corti.ai/registry/dedalus/coding-expert.svg",
                    mimeType: "image/svg+xml",
                    sizes: ["any"],
                },
            ],
            provider: "Dedalus",
            websiteUrl: "https://dedalus.example.com/coding-expert",
            documentationUrl: "https://docs.dedalus.example.com/coding-expert",
            capabilities: {
                streaming: true,
                inputModes: ["text/plain"],
                outputModes: ["text/plain", "application/json"],
                tools: ["lookup_icd10", "validate_code"],
            },
            tags: ["icd10", "billing", "expert"],
            configSchema: {
                key: "value",
            },
        });
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/agentic/registry/connectors/connectorId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.registry.get("connectorId");
        }).rejects.toThrow(Corti.UnauthorizedError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/v2/agentic/registry/connectors/connectorId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.registry.get("connectorId");
        }).rejects.toThrow(Corti.NotFoundError);
    });
});
