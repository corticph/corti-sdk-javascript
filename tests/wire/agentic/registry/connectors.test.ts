// This file was auto-generated by Fern from our API Definition.

import * as Corti from "../../../../src/api/index";
import { CortiClient } from "../../../../src/Client";
import { mockServerPool } from "../../../mock-server/MockServerPool";
import { mockOAuth } from "../../mockAuth";

describe("ConnectorsClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = {
            connectors: [
                {
                    id: "pubmed-expert",
                    type: "registry",
                    name: "pubmed-expert",
                    title: "ICD-10 Coding Expert",
                    description: "description",
                    version: "1.4.2",
                    icons: [{ src: "src", mimeType: "image/svg+xml", sizes: ["48x48"] }],
                    provider: "Corti",
                    websiteUrl: "websiteUrl",
                    documentationUrl: "documentationUrl",
                    tags: ["tags"],
                    configSchema: { key: "value" },
                },
            ],
            nextPageToken: "nextPageToken",
            totalSize: 42,
        };

        server
            .mockEndpoint({ once: false })
            .get("/agentic/registry/connectors")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            connectors: [
                {
                    id: "pubmed-expert",
                    type: "registry",
                    name: "pubmed-expert",
                    title: "ICD-10 Coding Expert",
                    description: "description",
                    version: "1.4.2",
                    icons: [
                        {
                            src: "src",
                            mimeType: "image/svg+xml",
                            sizes: ["48x48"],
                        },
                    ],
                    provider: "Corti",
                    websiteUrl: "websiteUrl",
                    documentationUrl: "documentationUrl",
                    tags: ["tags"],
                    configSchema: {
                        key: "value",
                    },
                },
            ],
            nextPageToken: "nextPageToken",
            totalSize: 42,
        };
        const page = await client.agentic.registry.connectors.list();

        expect(expected.connectors).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.connectors).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/registry/connectors")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.registry.connectors.list();
        }).rejects.toThrow(Corti.UnauthorizedError);
    });

    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = {
            id: "pubmed-expert",
            type: "registry",
            name: "pubmed-expert",
            title: "PubMed",
            description: "Search PubMed for scientific articles, abstracts, and citations.",
            version: "1.0.0",
            icons: [
                {
                    src: "https://cdn.corti.ai/registry/corti/pubmed-expert.svg",
                    mimeType: "image/svg+xml",
                    sizes: ["any"],
                },
            ],
            provider: "Corti",
            websiteUrl: "https://corti.example.com/pubmed-expert",
            documentationUrl: "https://docs.corti.example.com/pubmed-expert",
            capabilities: {
                streaming: false,
                inputModes: ["text/plain"],
                outputModes: ["text/plain", "application/json"],
                tools: ["search_pubmed", "get_pubmed_abstract"],
            },
            tags: ["pubmed", "literature", "expert"],
            configSchema: { key: "value" },
        };

        server
            .mockEndpoint()
            .get("/agentic/registry/connectors/connectorId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.agentic.registry.connectors.get("connectorId");
        expect(response).toEqual({
            id: "pubmed-expert",
            type: "registry",
            name: "pubmed-expert",
            title: "PubMed",
            description: "Search PubMed for scientific articles, abstracts, and citations.",
            version: "1.0.0",
            icons: [
                {
                    src: "https://cdn.corti.ai/registry/corti/pubmed-expert.svg",
                    mimeType: "image/svg+xml",
                    sizes: ["any"],
                },
            ],
            provider: "Corti",
            websiteUrl: "https://corti.example.com/pubmed-expert",
            documentationUrl: "https://docs.corti.example.com/pubmed-expert",
            capabilities: {
                streaming: false,
                inputModes: ["text/plain"],
                outputModes: ["text/plain", "application/json"],
                tools: ["search_pubmed", "get_pubmed_abstract"],
            },
            tags: ["pubmed", "literature", "expert"],
            configSchema: {
                key: "value",
            },
        });
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/registry/connectors/connectorId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.registry.connectors.get("connectorId");
        }).rejects.toThrow(Corti.UnauthorizedError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .get("/agentic/registry/connectors/connectorId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.agentic.registry.connectors.get("connectorId");
        }).rejects.toThrow(Corti.NotFoundError);
    });
});
