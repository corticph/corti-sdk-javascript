// This file was auto-generated by Fern from our API Definition.

import { CortiClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";
import { mockOAuth } from "./mockAuth";

describe("NewSectionVersionsClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        server.mockEndpoint().get("/new/sections/sectionID/versions").respondWith().statusCode(200).build();

        const response = await client.newSectionVersions.list("sectionID");
        expect(response).toEqual(undefined);
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        server.mockEndpoint().post("/new/sections/sectionID/versions").respondWith().statusCode(200).build();

        const response = await client.newSectionVersions.create("sectionID");
        expect(response).toEqual(undefined);
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        server.mockEndpoint().get("/new/sections/sectionID/versions/versionID").respondWith().statusCode(200).build();

        const response = await client.newSectionVersions.get("sectionID", "versionID");
        expect(response).toEqual(undefined);
    });

    test("delete", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        server
            .mockEndpoint()
            .delete("/new/sections/sectionID/versions/versionID")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.newSectionVersions.delete("sectionID", "versionID");
        expect(response).toEqual(undefined);
    });

    test("publish", async () => {
        const server = mockServerPool.createServer();
        mockOAuth(server);

        const client = new CortiClient({
            maxRetries: 0,
            clientId: "client_id",
            clientSecret: "client_secret",
            tenantName: "test",
            environment: { base: server.baseUrl, wss: server.baseUrl, login: server.baseUrl, agents: server.baseUrl },
        });

        server
            .mockEndpoint()
            .post("/new/sections/sectionID/versions/versionID/publish")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.newSectionVersions.publish("sectionID", "versionID");
        expect(response).toEqual(undefined);
    });
});
