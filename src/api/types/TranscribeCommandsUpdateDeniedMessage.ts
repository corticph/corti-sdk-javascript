// This file was auto-generated by Fern from our API Definition.

import type * as Corti from "../index.js";

export interface TranscribeCommandsUpdateDeniedMessage {
    type: "commands_update_denied";
    /** The reason the update was denied. Possible values include "No active session. Send a valid config message first.", "unknown parameter(s)", "ineligible parameter(s): <list>", and "invalid definition". */
    reason?: string;
    /** List of invalid field names that caused the rejection, when applicable. */
    fields?: string[];
    /** The session ID. */
    sessionId: string;
    /** Empty array. The existing command configuration is unchanged. */
    commands: Corti.TranscribeCommand[];
}
