# Specify files that shouldn't be modified by Fern

# Custom code (not generated by Fern)
src/custom/

# Patched to use CortiAuth (real token endpoint) instead of AuthClient (fake-token)
src/auth/OAuthAuthProvider.ts
